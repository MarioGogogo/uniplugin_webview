# UniWebView 自定义基座打包指南

## 插件文件结构

```
uniplugin_webview/
├── package.json              # 插件配置文件（自定义基座必需）
├── _config.xml               # 插件配置文件（部分版本需要）
├── build.gradle              # 模块构建配置
├── proguard-rules.pro        # ProGuard 规则
├── README.md                 # 使用说明
├── 调用指南.md                # API 文档
└── src/main/
    ├── AndroidManifest.xml   # 清单文件
    └── java/uni/dcloud/io/uniplugin_webview/
        ├── WebViewComponent.java  # WebView 组件
        └── WebViewModule.java     # WebView 模块
```

---

## 方式一：Android Studio 离线打包自定义基座

### 步骤 1: 准备环境

1. Android Studio Arctic Fox 或更高版本
2. JDK 1.8 或更高版本
3. uni-app SDK（在 `app/libs/` 目录下）

### 步骤 2: 导入插件模块

1. 在 Android Studio 中打开项目
2. 确保 `uniplugin_webview` 模块已导入
3. 检查 `settings.gradle` 包含：
   ```gradle
   include ':uniplugin_webview'
   ```

### 步骤 3: 检查依赖配置

确保 `app/build.gradle` 中包含：
```gradle
dependencies {
    implementation project(':uniplugin_webview')
}
```

### 步骤 4: 检查插件注册配置

确保 `app/src/main/assets/dcloud_uniplugins.json` 中注册了插件：
```json
{
  "nativePlugins": [
    {
      "plugins": [
        {
          "type": "component",
          "name": "UniWebView",
          "class": "uni.dcloud.io.uniplugin_webview.WebViewComponent"
        }
      ]
    },
    {
      "plugins": [
        {
          "type": "module",
          "name": "UniWebViewModule",
          "class": "uni.dcloud.io.uniplugin_webview.WebViewModule"
        }
      ]
    }
  ]
}
```

### 步骤 5: 添加权限

确保 `AndroidManifest.xml` 中包含必要权限：
```xml
<uses-permission android:name="android.permission.INTERNET"/>
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
```

### 步骤 6: 构建 APK

1. 点击 `Build` -> `Generate Signed Bundle / APK`
2. 选择 APK 格式
3. 使用你的签名密钥（或使用测试密钥）
4. 构建发布版 APK

---

## 方式二：HBuilderX 云打包自定义基座

### 前置条件

1. 已将插件上传到 HBuilderX
2. 已在 `manifest.json` 中配置插件

### 步骤 1: 准备插件包

确保插件目录包含以下文件：
- `package.json`（必需）
- `src/` 目录（包含原生代码）

### 步骤 2: 在 HBuilderX 中配置

1. 打开 uni-app 项目
2. 右键点击 `uni_modules` 目录
3. 选择 `新建 uni_modules 插件`
4. 将插件文件复制到插件目录

### 步骤 3: 配置 manifest.json

在 `manifest.json` -> `App 原生插件配置` 中添加：
```json
{
  "app-plus": {
    "nativePlugins": {
      "UniWebView": {
        "__plugin_info__": {
          "name": "UniWebView",
          "description": "Android WebView 原生插件",
          "platforms": ["Android"]
        }
      }
    }
  }
}
```

### 步骤 4: 打包自定义基座

1. 在 HBuilderX 中点击 `发行` -> `原生 App-离线打包`
2. 选择 `打包自定义基座`
3. 等待打包完成

### 步骤 5: 测试基座

1. 将自定义基座安装到手机
2. 在 HBuilderX 中运行项目到自定义基座
3. 测试 WebView 功能

---

## 方式三：使用 uni_modules 方式集成

### 步骤 1: 创建 uni_modules 目录结构

```
uni_modules/
└── uniplugin-webview/
    ├── package.json
    ├── plugin.xml
    ├── index.uts (或 index.js)
    └── static/
```

### 步骤 2: 创建 package.json

```json
{
  "id": "uniplugin-webview",
  "displayName": "UniWebView",
  "version": "1.0.0",
  "description": "Android WebView 原生插件",
  "keywords": ["webview", "jsbridge"],
  "repository": "",
  "engines": {
    "HBuilderX": "^3.8.0"
  },
  "uni-app": {
    "android": {
      "plugins": [
        {
          "type": "component",
          "name": "UniWebView",
          "class": "uni.dcloud.io.uniplugin_webview.WebViewComponent"
        },
        {
          "type": "module",
          "name": "UniWebViewModule",
          "class": "uni.dcloud.io.uniplugin_webview.WebViewModule"
        }
      ]
    }
  }
}
```

### 步骤 3: 创建 plugin.xml

```xml
<plugin version="1.0">
  <feature name="UniWebView">
    <module name="UniWebViewModule" class="uni.dcloud.io.uniplugin_webview.WebViewModule"/>
    <component name="UniWebView" class="uni.dcloud.io.uniplugin_webview.WebViewComponent"/>
  </feature>
</plugin>
```

### 步骤 4: 创建接口文件 (index.uts)

```uts
// 声明 WebView 组件
export const UniWebView = {
  reload: () => {},
  goBack: () => {},
  goForward: () => {},
  getCurrentUrl: (): string => '',
  evaluateJavascript: (js: string, callback: (result: any) => void) => {},
  clearCache: () => {}
}

// 声明 WebView 模块
export const UniWebViewModule = {
  clearAllCache: (callback: (result: any) => void) => {},
  setCookie: (options: {url: string, name: string, value: string}, callback: (result: any) => void) => {},
  getCookie: (options: {url: string}, callback: (result: any) => void) => {},
  removeCookie: (options: {url: string, name: string}, callback: (result: any) => void) => {},
  clearAllCookies: (callback: (result: any) => void) => {},
  openInBrowser: (options: {url: string}, callback: (result: any) => void) => {},
  getWebViewVersion: (callback: (result: any) => void) => {}
}
```

### 步骤 5: 打包发布

1. 在 HBuilderX 中右键点击 `uni_modules/uniplugin-webview`
2. 选择 `上传插件到插件市场`
3. 填写插件信息并提交审核

---

## 常见问题

### Q1: 打包后插件不生效？
**A:** 检查以下几点：
1. `dcloud_uniplugins.json` 是否正确配置
2. `package.json` 是否存在且格式正确
3. 插件类路径是否正确
4. 是否重新同步了 Gradle

### Q2: HBuilderX 提示找不到插件？
**A:** 
1. 确保 `package.json` 中的 `uni-app.plugins` 配置正确
2. 重启 HBuilderX
3. 清理缓存后重新打包

### Q3: 自定义基座闪退？
**A:** 
1. 检查 Logcat 日志
2. 确认所有依赖库已正确引入
3. 检查权限配置是否完整
4. 确认 `AndroidManifest.xml` 配置正确

### Q4: 如何调试自定义基座？
**A:** 
1. 在 Android Studio 中运行 `app` 模块
2. 使用 `adb logcat` 查看日志
3. 在 uni-app 中使用 `console.log` 输出调试信息

---

## 必要文件清单

打包自定义基座前，请确保以下文件存在且配置正确：

### 插件模块目录（uniplugin_webview/）
- [x] `package.json` - 插件配置（必需）
- [x] `_config.xml` - 插件配置（部分情况需要）
- [x] `build.gradle` - 构建配置
- [x] `src/main/AndroidManifest.xml` - 清单文件
- [x] `src/main/java/.../WebViewComponent.java` - 组件实现
- [x] `src/main/java/.../WebViewModule.java` - 模块实现

### 应用目录（app/）
- [x] `src/main/assets/dcloud_uniplugins.json` - 插件注册
- [x] `build.gradle` - 包含插件依赖
- [x] `src/main/AndroidManifest.xml` - 包含权限声明

### 项目根目录
- [x] `settings.gradle` - 包含插件模块

---

## 技术支持

- uni-app 官方文档：https://uniapp.dcloud.net.cn/
- 插件开发指南：https://nvue.dcloud.net.cn/
- DCloud 论坛：https://ask.dcloud.net.cn/
